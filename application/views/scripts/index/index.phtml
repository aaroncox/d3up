<div class="content-sidebar">
	<?= $this->render("./index/_sidebar.phtml") ?>
</div>
<div class="home content-page with-sidebar ui-helper-clearfix">
  <?= $this->render("./index/widgets/video-builds.phtml") ?>
  <?= $this->render("/index/widgets/guides.phtml")?>
  <!-- <table id="recent-builds" class="d3up-table">
    <thead>
      <tr>
        <td colspan="10"><h5 style="color: #B18135">Recent Builds</h5></td>
      </tr>
    </thead>
    <? 
			//$this->render("./index/widgets/recent-builds.phtml")
			?>
  </table> -->
</div>
<script type="text/javascript" charset="utf-8">
  $(function() {
    $("td.skills img").each(function() {
  		if($(this).data("id") && $(this).data("id") != "undefined") {
  			var skill = d3up.gameData.actives[$(this).attr("data-class")][$(this).attr("data-id")];
  			if(!skill) {
  				skill = d3up.gameData.passives[$(this).attr("data-class")][$(this).attr("data-id")];
  				if(skill) {
  					skill.name = $(this).attr("data-id").replace(/\-/g," ").capitalize();					  
  				}
  			}
  			if(skill) {
  				var tooltip = skill.desc;
  				if(skill.rune) {
  					tooltip = tooltip + "<br/><br/>" + skill.rune;
  				}
  				$(this).attr("data-name", skill.name);
  				$(this).attr("data-tooltip", tooltip);
  				$(this).bindSkilltip();				
  			}				
  		}
  	});    
  });
</script>