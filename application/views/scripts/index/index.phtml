<style type="text/css" media="screen">
	.home {
		margin: 10px;
	}
	.recent-items {
		width: 600px;
		padding: 1px;
	}
	.info {
		float: right;
		width: 300px;
	}
</style>
<div class="home">
	<div class="info">
		<h2>What is D3Up?</h2>
		<p>D3Up is designed as a tool to help you find information about items, a place to create a character profile, and compare items to determine which stats of yours will change.</p>
		<h2>What can I do here?</h2>
		<ul>
			<li><a href="/item/create">Create Items</a> to share with others.</li>
			<li>Make <a href="/hero/create">character profiles</a> with detailed statistics!</li>
			<li>Compare the items you're wearing to other items you've created.</li>
		</ul>
	</div>
	<div class="recent-items">
		<h2>Recently Created Items</h2>
		<ul>
			<? foreach($this->items as $item): ?>
				<li>
					<?= $this->itemLink($item) ?> (<?= ucwords(str_replace("-", " ", $item->type))?>)
				</li>
			<? endforeach; ?>
		</ul>
		<?php echo $this->paginationControl($this->items,
		                                    'Elastic',
		                                    'index/_pages.phtml'); ?>
	</div>
	<? if($this->heroes->count()): ?>
	<div class="recent-heroes">
		<h2>Recently Created Heroes</h2>
		<ul>
			<? foreach($this->heroes as $hero): ?>
				<li>
					<?= $this->mLink($hero) ?> (<?= ucwords(str_replace("-", " ", $hero->class))?>)
				</li>
			<? endforeach; ?>
		</ul>
		<?php echo $this->paginationControl($this->heroes,
		                                    'Elastic',
		                                    'index/_pages.phtml'); ?>
	</div>
	<? endif; ?>
</div>